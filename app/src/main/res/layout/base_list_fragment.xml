<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/baseListFragmentSwipeLayout">

        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/baseListFragmentRecyclerView"/>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <TextView
        style="@style/TextAppearance.MaterialComponents.Subtitle1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/baseListFragmentNothingFound"
        android:text="@string/nothing_found"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_margin="36dp"
        android:visibility="gone"/>

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/baseListFragmentAdd"
        android:layout_gravity="bottom|end"
        android:text="@string/add"
        android:layout_marginBottom="24dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:textColor="@color/colorPrimary"
        android:fontFamily="@font/rubik"
        android:textAppearance="@style/TextAppearance.AppTheme.Rubik"
        app:rippleColor="@color/colorPrimaryLight"
        app:iconTint="@color/colorPrimary"
        app:icon="@drawable/ic_add"
        app:backgroundTint="@color/colorSurface"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>